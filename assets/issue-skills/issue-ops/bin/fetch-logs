#!/bin/sh
# 架构自适应入口:选对本平台的引擎后原样透传参数与退出码。
# 用法见同目录 SKILL.md;密码经 FETCH_LOGS_PASSWORD 环境变量传入。
set -e
ARCH=$(uname -m)
case "$ARCH" in
  x86_64) BIN=fetch-logs-linux-amd64 ;;
  aarch64|arm64) BIN=fetch-logs-linux-arm64 ;;
  *) echo "不支持的架构: $ARCH" >&2; exit 2 ;;
esac
exec "$(dirname "$0")/$BIN" "$@"
