# 问题处理(Issue Flow)

围绕一个"我的问题"(DTS 单或自研)的处理会话域:AI 自主编排、按需调用平台工具,可能修改零个或多个代码仓。

## Language

**问题会话**:
围绕一个"我的问题"展开的多轮处理会话。研究后无代码修改也是合法终点(非问题出口)。

**登记元信息**:
手工登记时用户填写的全部信息的完整存留:标题、现象描述、业务模块、带出的仓、网管环境。每回合全量进 AI 上下文,也可随时用获取元信息工具重查;与按单号拉取的问题单详情(dts_get_ticket)是两回事——一个是人填的,一个是平台拉的。
_Avoid_: 备注字段、表单残留

**业务模块**:
知识飞轮维护的团队资产:具名业务单元及其绑定的代码仓(至少 1 个)。无单登记时仓的唯一来源。
_Avoid_: 手工填仓(2026-08-29 废除)

**网管环境**:
问题发生所在的网络管理系统现场:登记页每个问题填写一个网管环境IP,运维工具/API 仍可显式处理多台;另有页面账号与页面密码、后台密码。后台密码供抓日志,页面凭据供页面操作;两者均可进入当前问题的 AI 上下文(ADR-0003)——它管的是现场公开口令,不是平台凭据。
_Avoid_: 服务器地址、共用密码(旧称)

**关联仓**:
登记进问题会话的全部代码仓,彼此平等——都可研读,也都可修改。
_Avoid_: 参考仓、主仓、从仓(2026-08-28 废除的等级分法)

**变更仓**:
AI 实际修改并选择交付的关联仓。判定权归 AI(它对哪些仓调用交付工具);平台不做机械 diff。

**拉仓**:
AI 调用平台工具把一个代码仓加入会话并克隆。克隆在宿主侧带凭据执行,AI 只见结果事实(目录、分支、基线命中与否)。

**一仓一 MR**:
每个变更仓各自建分支、各自提 MR;问题闭环 = 变更仓的 MR 全部建好且流水线全绿,用户归档收口(ADR-0013:全绿即流程终点,归档前续聊即返工)。

**阶段是会话级的**:
固定流程的阶段属于会话,不按仓复制;多仓差异藏在阶段内部的逐仓处理里。

**举卡**:
平台(或 Agent 经 AskUserQuestion)把一个决策以问题卡形式举到用户面前,会话挂起等答复。举卡是回合停机的合法位置之一。手机端(小鲁班纯文本回复)与页面作答同一裁决口——等待卡进审批册、审批码与通知同源,作答仍走 answer() 单点分派(2026-09-02 与需求侧手机审批对齐)。

**红灯人工闸**:
问题会话的流水线红灯无法自动修复时举的平台闸,两种形态:不可修告警闸(红灯全部来自部署名单内的平台侧工具告警,人处理/豁免后作答,平台重置监看重看同一提交)与报错回灌闸(红灯拿不到可定位原文,人把报错原文粘贴进卡作答,原文作为人工证据注入下一修复回合)。都是人工事实,月光永不代答。
_Avoid_: 猜改(证据缺口维度不许盲改,这正是举卡的由来)

**AI 推荐**:
选项题举卡时 AI 必须指明的倾向项——结构化字段,机械校验必须命中所给选项之一。推荐不代替选择:卡上只标注、不预选,拍板权始终在用户;平台闸的推荐由宿主码表或 AI 提案派生,不是 AI 现场给的。推荐是将来无审批全自动模式的执行依据,所以必有、且必须结构化。

**提前收嘴**:
模型在阶段未走到出口时自行结束回合(输出总结文本、不再调工具)。阶段真相在平台,收嘴不改变阶段;平台以催办续跑把它推回阶段(每回合预算内),连收嘴即转人工。

**出口**:
固定阶段收口的唯一动作,全流程只有两种形态:四个阶段(拉单/拉仓/修复/提交MR)的出口是 complete_stage 自报;两个阶段(问题分析/无单结论)的出口是举卡工具本身——卡即出口,没有 complete_stage 可绕。催办判据 = 未到出口。

**自报推进**:
AI 判断阶段目标已达成,调 complete_stage 宣布收口,平台记录并推进。平台不核实 AI 的工作事实(单读没读完、仓拉没拉齐、码改没改完——那是 AI 的裁量),只保留它做得比 AI 好的两道核验:用户决策卡与 MR 验绿门。

**事实上报**:
report_ut 的语义——UT 并入问题修复阶段(2026-09-02,TDD:先写复现单测再改码),修复过程中 AI 跑完测试自愿上报结构化结果(结论/摘要/日志落点),平台只记账(现场记录可查),不推进阶段、不设门禁。

**MR 验绿门**:
提交 MR 阶段的平台硬卡:AI 申报的 MR 清单必须与会话台账(平台在创建 MR 时自动记的账)一致,且每个 MR 最新推送的流水线全绿,阶段转移才发生。有红当场打回,在跑受理等绿——绿永远是宿主监看器说了算,不是 AI 说了算。

**整体回退**:
检视提交时平台的处置:问题分析及之后的阶段标记重做、回退轮次加一,已申报的 UT/流水线/MR 账作废;分支与 MR 延用——同分支追加修复,不推倒重来。换库验证的机器触发随换库封存移除(ADR-0013);收口后的续聊返工不是回退——重开当前阶段,轮次账不动。

**检视**:
用户针对分析报告提出的修订意见及触发的重跑行为。检视对象只有分析报告,不含代码——代码层面的意见走工作区变更的请 AI 复核。提交检视走整体回退(唯一触发源,人工)。转正后的会话不可再检视;自由模式的会话没有阶段机可退,检视降级为普通插话。
_Avoid_: 审查、复核(与请 AI 复核混称)、批注(那是需求流检视批注的词)

**月光模式**:
个人设置「人工介入程度」的过程轴(moonlight):开着时过程人工节点由系统代答放行——需求流代答认得出的确认类选项(认不出不猜),问题流代答分析结论闸(有单全量、无单仅非问题+高置信),并按推荐项整卡代答 Agent 自举的纯选项题问答卡(开放题、混卡、检视回合的卡永不,ADR-0006);每处现读现判,随时开改,已挂起的卡不追溯代答。问用户事实的闸(环境配置、验证结果)与 MR 人工合入永不代答。
_Avoid_: 全自动档(前端把月光与交付过目合成的四档旋钮名,不落盘)

**推送前过目**:
个人设置「人工介入程度」的交付轴(push_confirmation,真人缺省即开):开启后问题会话里推送分支不再是直推——push_branch 被平台举起「推送确认」卡(带服务端现查仓库生成的变更摘要),确认产一次性令牌放行一次推送,下次推送重新过目;答「暂不推送」不产令牌,AI 按意见调整。固定与自由两模式同过这道闸;月光不代它——过目是用户显式开启的意志,更具体的意志赢(ADR-0009,与需求流 push 前确认同一裁定)。
_Avoid_: push 前确认(那是个人设置与需求流的叫法,问题流域内指同一开关)、MR 合入确认(合入永远人工,与过目是两道门)

**模块预绑**:
单号→业务模块的人工绑定(DTS 列表每行的模块下拉,选即存,团队共享人人可写):发起时会话静默携带预绑模块与它的仓,AI 跳过检索识别直接拉仓。人工选定的模块对 AI 锁死——bind_module 工具级拒绝改绑,发现不符只能问人(ADR-0010);服务端按单名的自动匹配仍是猜测,不锁。改绑只影响下一次发起,进行中会话不追溯。临时方案:上游 DTS 原生提供模块字段后整体退役。
_Avoid_: AI 识别绑定的模块(那是运行时检索,与预绑相对)、模块归属审核(人人可写,无审批)

**现场记录**:
一个问题会话的全部执行事实记录:事件流(工具调用及其输入输出、AI 发言、用户指令与决策)加会话台账(单号、仓、阶段推进、结论)。它是复盘与问题定位的原始凭证——现场页签是它的直播视图,导出文件是它的快照。

**查看模式**:
非归属人打开他人问题会话工作台的形态:全部现场对登录开发者可见(概要、时间线、材料、事件流),零操作——续聊、作答、取消/归档、绑单、检视、材料修改等一切写动作仍归归属人。与需求流"人人可看、归属人操作"同一约定;管理员的只读是既有角色边界,不是这个词条的延伸。
_Avoid_: 协作模式(没有第二写入者)、只读会话(会话本身照常在跑,只是看的人不能动)

**过程问答**:
现场记录面向复盘的投影:只呈现 agent 的提问(举卡)、用户对卡与平台闸的答复、用户的主动插话;agent 的过程性发言不进入。只追加、不回写——agent 修订分析报告也不改写已有问答,新事实以新回合出现。
_Avoid_: 对话记录、聊天记录(它不是完整对话)

**通用定位 skill**:
团队货架发布的、未限定作用域的 engineering 知识资产,对所有问题会话可见。定位方法论的第一顺位供给;货架无匹配时退到仓内 issue-research,再无则 agent 自行定位——取用是替换式的,不叠加。
_Avoid_: 把仓内 issue-research 称作通用定位 skill(它只是兜底)

**业务问题 skill**:
业务代码仓随仓维护的问题定位知识,约定放在仓根目录 .cac/skills/ 的标准 skill 目录形态(与需求侧仓内能力发现同一约定),拉仓后按需阅读。它供的是领域事实与排障方法;流程、报告格式、停机节奏由问题分析工作流的平台契约决定,不由它约定。

**业务知识**:
问题分析消费的领域事实库(ADR-0012),两个来源一张地图:①团队资产库按绑定模块定格的已发布知识(只读投影 .mae-flow-work/business-modules/,台账防版本漂移);②仓内业务知识(已拉仓根目录 docs/ 目录树,渲染时现扫,40 条上限超限折叠)。正文一律按需 Read,只供业务事实不定流程;不举卡、不分介入档。服务复述现象、取证与报告引用(业务事实出处写文件路径)。
_Avoid_: 业务模块知识(那是需求侧对同一资产库的消费形态名)、业务问题 skill(skill 供排障方法论,业务知识供领域事实,互补不互斥)

**业务知识树**:
业务仓 docs/ 下用文件路由组织的分层知识(总览→仓内模块,纯仓内资产):地图现扫只列一层(文件与一级子目录),深层靠目录折叠与按需 Read;平台对树的内部组织无感知、无约定。
_Avoid_: 业务知识(那是两源一张地图的整体)、模块知识(跨仓粘合层,存放处不同)

**模块总览**:
跨仓业务模块在资产库发布的一篇总览知识:多仓同模块的粘合层,模块 Owner 维护,analyze 随模块定格。单仓模块不设——仓概览即总览。
_Avoid_: 业务总览(已拆分为模块总览与仓概览,勿再单用)、仓概览(那是仓根一层)

**仓概览**:
业务仓根 CLAUDE.md/AGENTS.md 承载的仓内概况:问题会话 cwd 在会话根,平台不自动加载仓根文件,由取证指引按需 Read;缺席回退在 docs/ 下自行查找。
_Avoid_: 自动加载(平台从不为 repo/<仓名>/ 触发内核 context 装载)、模块总览

**skill 圈选**:
固定流程进入问题分析时的人为知识圈定,只在月光关档发生(ADR-0011):平台扫描已拉仓的 .cac/skills,举多选卡由归属人圈定必读集合,空选=AI 按取用次序自主。圈定的是必读而非白名单——选定的必须读,读完仍可按既有次序补充;集合随台账继承,重走分析不重举。月光开档与自由模式不圈,维持 AI 自主。
_Avoid_: skill 加载(平台从不装载业务仓 skill,Agent 始终自己读)、白名单(那是封闭集,圈选是必读集)

**轻量路径**:
问题分析工作流为简单问题(已知问题、一眼可定位的配置错误)预留的最短路径:工单信息加业务问题 skill 直接命中即达结论。走不走由 agent 判断;报告出简版,但要素不缺。

**分析报告**:
问题分析阶段的核心交付物,落工作区根(issue-analysis.md):首行一句话总结(串联 现象→根因→方案,只读这行就能拍板),正文五节——问题现象/问题根因/修改方案/证据链/置信度。证据链每条一行=事实+出处指针(原文不贴,只收支撑根因与方案的证据,上限5条,反证必须有一条或声明没有)——它的读者是下一棒的 AI,证明"证据真被找过";修改方案是 fix 阶段的执行承诺,不是建议。提交的是收敛后的结论版;续聊继承、无单转正、复盘检视都认它。
_Avoid_: 无一句话总结的开头;日志整段摘录、过程回放式长文、只拣顺证据的证据链(2026-09-03 定型:现象-根因-方案三段式解禁为章节结构——2026-08-31 曾避它,病根其实在长度不在结构;「下一步建议」并入修改方案,从建议升格为承诺)

## Workflow

**pr**:
用户说「pr」「pr合并」「pr 合入 main」的既定语义:推送当前分支后建 PR 并直接合入 main,一气呵成,不必再等第二次确认。「pr 合并到 main」与「pr合并」同义;只建不合说「建 PR 不合」。
_Avoid_: 建 PR 后停下等拍板(那是「建 PR 不合」的显式要求)、本地直接 push main(永远走 PR)
