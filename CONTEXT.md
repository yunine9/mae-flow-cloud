# 问题处理(Issue Flow)

围绕一个问题(DTS 单或自研)的处理会话域:测试登记、开发责任人推进,AI 自主编排、按需调用平台工具,可能修改零个或多个代码仓。

## Language

**问题会话**:
围绕一个问题(DTS 单或自研)展开的多轮处理会话,登记人登记、责任人负责推进。研究后无代码修改也是合法终点(非问题出口)。

**登记人**:
登记问题会话的人,通常是测试:发现并描述问题,选定业务模块与网管环境,指派责任人。登记完成即撒手——对会话零操作(查看模式),登记的问题就在「我的问题」列表跟踪(归属或登记人是自己;2026-09-16 修订:不再分「我负责的/我登记的」两个范围);问题的修复验收不归登记人,验证卡答在责任人手里。
_Avoid_: 创建人(登记人未必是会话归属)

**责任人**:
问题会话的归属人与唯一推进者:续聊、作答、绑单、取消/归档等一切写动作归责任人,闸口通知、Git 提交身份、介入档位均随责任人走。登记时指派,缺省取所选业务模块的模块责任人,登记人可改;自登记(登记人=责任人)时省指派通知。
_Avoid_: 模块责任人(那是资产归属,会话责任人由它缺省而来,可改选)、任务责任人(任务流的概念)、把责任人叫创建人(归属语义已从创建人改为责任人)

**登记元信息**:
手工登记时用户填写的全部信息的完整存留:标题、现象描述、业务模块、带出的仓、网管环境、登记人与责任人。每回合全量进 AI 上下文,也可随时用获取元信息工具重查;与按单号拉取的问题单详情(dts_get_ticket)是两回事——一个是人填的,一个是平台拉的。
_Avoid_: 备注字段、表单残留

**业务模块**:
知识飞轮维护的团队资产:具名业务单元及其绑定的代码仓(至少 1 个)。无单登记时仓的唯一来源;运行中用户可经元信息页签指派补充仓(ADR-0023),模块绑定仓因此不可移除。
_Avoid_: 登记页手工填仓(2026-08-29 废除;2026-09-11 起运行中经元信息页签的用户指派是补充通道而非替代,ADR-0023)

**模块责任人**:
业务模块资产的归属人:创建模块时指定,只有管理员能转移;模块维护者协助管理(可改模块、发布知识,转移责任人与归档模块不行)。问题登记时会话责任人的缺省来源——模块指向谁,问题就流向谁。
_Avoid_: 会话的责任人(问题会话的归属,由模块责任人缺省而来,登记人可改)、Owner(中文名统一叫模块责任人)

**网管环境**:
问题发生所在的网络管理系统现场:登记页从「环境管理」选一台环境(以 IP 为键);后台密码供抓日志与换库部署,可进入当前问题的 AI 上下文(ADR-0003)——它管的是现场公开口令,不是平台凭据。页面账号/密码已废弃(2026-09-10:流程只登录后台,不登录网管页面)。

**环境形态**:
一套网管环境的部署形态,人工二选一:虚拟化(经网管节点发现后台节点抓日志)或容器化(经 OM 节点 kubectl 发现 Pod 抓日志)。登记页或环境配置卡上由人选定,决定日志抓取走哪套引擎;AI 只读不猜、不试错——元信息里没有就举卡要,不要两种都试。
_Avoid_: 环境类型(与"页面/后台"那组凭据区分度不够)、让 AI 按报错试探形态(浪费且易误判)
_Avoid_: 服务器地址、共用密码(旧称)

**环境管理**:
全局维护的网管环境台账,一条记录一个环境(以 IP 为唯一键):部署形态、端口、sopuser 三账号密码、可选的独立 root 密码、自定义标签与探活状态。会话需要环境时从台账选一条,取的是选定时点的快照——台账后续怎么改,都不影响已进行的会话。
_Avoid_: 环境配置(那是会话里举的闸卡)、环境库、环境列表

**合入事实**:
平台远端 MR 的真实状态(merged/closed)与源提交 SHA,由监听轮询向平台读取。问题会话的「已交付」结论只认合入事实:全部 MR merged 才算交付;验绿、建了 MR、推送过都只是过程事实。SHA 以平台返回为准,不要求与验绿 SHA 相同。
_Avoid_: 验绿即交付、有 MR 即交付、拿本地台账推断交付结论

**关联仓**:
登记进问题会话的全部代码仓,彼此平等——都可研读,也都可修改。
_Avoid_: 参考仓、主仓、从仓(2026-08-28 废除的等级分法)

**变更仓**:
AI 实际修改并选择交付的关联仓。判定权归 AI(它对哪些仓调用交付工具);平台不做机械 diff。

**Agent 主理**(项目原则,2026-09-11 立):
不涉及安全风险的前提下,运行时状态变更一律由 Agent 经工具执行,UI 只做状态展示。平台侧只做校验、留痕与通知投递,不代执;清单/状态随 Agent 执行结果呈现,前端不做乐观更新。首例:问题会话仓清单的用户指派增删(ADR-0023)。

**拉仓**:
AI 调用平台工具把一个代码仓加入会话并克隆。克隆在宿主侧带凭据执行,AI 只见结果事实(目录、分支、基线命中与否)。

**知识仓**:
配置中心管理员页签指定的全局领域知识代码仓:团队全部领域知识统一在一个 git 仓里治理(git 治理是既定前提,不再议)。问题会话开工即随关联仓克隆进 repo/ 平铺目录,但它不是关联仓——只读参考件:可研读,不可修改、不可交付。仓内组织归仓主人治理,平台对目录结构零约定;AI 缺领域知识时先翻它的目录,按业务模块名(含缩写)优先检索相关知识。
_Avoid_: 关联仓(它是参考件,不可交付)、知识库资产(那是团队资产库的单篇发布形态,知识仓是整仓装载)、全局知识库(口语)

**拉取日志**:
把网管侧日志落到会话现场(local-logs)的动作,执行者始终是 Agent(经 issue-ops 技能,按环境形态走对应引擎);页面「拉取日志」按钮只是把拉取意图递给 Agent(Agent 主理第二例,ADR-0026),平台不代拉。人读日志只有下载一途(整包 zip);在线日志树/查看器/解压已随该 ADR 退役——AI 读日志不经页面,它自己拉自己读。
_Avoid_: 平台代拉、页面直接拉取(违反 Agent 主理);把页面拉取当 AI 读日志的前提(AI 的日志链路在容器与 local-logs,页签橱窗已拆)

**环境预热**:
拉仓收口进 analyze 时后台点火的专职编译会话(需求侧 warmupAgent 的问题流移植,2026-09-04):同一任务容器里编译基线、焐热分仓缓存、沉淀构建入口到 `.mae-flow-work/build-notes.md`。fail-open 旁路:失败落 infrastructure_failure 收据(`issue.json` 的 `warmup` 字段,不上 wire),主流程照走;与需求侧同条件启用(host + isolateImage),测试形态缺席即关。修复 Agent 开改前先读 build-notes。

**容器内超时**:
bash 与 ops 命令超时的统一语义(2026-09-04 收窄,AI bash 路径跟进 ops 的 7df0940 模式):命令包进容器内 coreutils `timeout`,超时只了结命令进程组、会话容器不动;exec 的「超时=销毁容器」退为预算+60s 兜底保险。此前一次 npm install 超时就掀掉容器,自愈重建让模型在无知中循环重试(2940 次实测)。Abort(用户打断)仍销毁容器。

**一仓一 MR**:
每个变更仓各自建分支、各自提 MR;问题闭环 = 变更仓的 MR 全部建好且流水线全绿,全部合入后自动归档收口(ADR-0013 全绿即流程终点 + ADR-0034 合入即归档:有单手动归档退役,取消=失败;归档前续聊即返工)。

**提交格式**:
修复分支上每个 git 提交的信息必须精确匹配 `[单号][类型] 描述`(如 `[DTS2026082001317][fix] 修复登录超时`),类型限 feat/fix/refactor/test/chore/docs/style,CodeHub pre-receive 钩子拒收不合规提交。管的是**分支上的每个提交**;别和「MR 标题」混——两条规则各管各的对象。

**MR 标题**:
CodeHub 要求 MR 标题与问题单标题**精确相等**,原样照抄、无前缀无后缀。create_mr 平台代笔(2026-09-07):按绑定单号现场查 DTS 取权威标题,不收 AI 传入、不拿会话登记标题顶替(单据中途改题也不漂);取不到如实打回(mr.title_missing),不静默降级。管的是**一仓一个的合并请求**;提交信息里的 `[单号]` 前缀是「提交格式」的事,与标题无关。

**阶段是会话级的**:
固定流程的阶段属于会话,不按仓复制;多仓差异藏在阶段内部的逐仓处理里。

**举卡**:
平台(或 Agent 经 AskUserQuestion)把一个决策以问题卡形式举到用户面前,会话挂起等答复。举卡是回合停机的合法位置之一。手机端(小鲁班纯文本回复)与页面作答同一裁决口——等待卡进审批册、审批码与通知同源,作答仍走 answer() 单点分派(2026-09-02 与需求侧手机审批对齐)。

**举卡发起权**(2026-09-13 拍板,ADR-0024):
何时举卡、举哪张,发起权全在 AI——回合内经举卡工具落卡,落卡即返回,裁决照旧开新回合。平台只做三样:前置事实校验(可拒绝举卡,判据只查平台可复核的事实,不查 AI 口供)、投递(监看事实三态送进会话,不代举)、计数与裁决(预算、码表、verdict 分派)。
_Avoid_: 平台代举(监看器直接落卡,2026-09-13 起废除);兜底举闸(以守闸器报警替代)

**守闸器**:
纯报警不举卡的平台监视:机械判据发现「应举的卡长时间缺席」(如全绿后无验证卡)即喊人(小鲁班)。阈值默认 120 分钟(管理页旋钮,0=关);判据排除已验证待合入的会话——它只报「欠卡」,不报「等人」或「已办结」。静默漏卡唯一的声器;与催办分工——催办把 AI 推回阶段,守闸器对人。
_Avoid_: watchdog(中文名);兜底举闸(它不举卡)

**红灯人工闸**:
问题会话的流水线红灯需人工介入时 AI 经 raise_gate 举的卡(2026-09-13 起 ADR-0024,平台不再代举),两种形态:不可修告警闸(红灯改代码解决不了,人在交付平台处理/豁免后作答,平台重置监看账重看同一提交)与报错回灌闸(报错原文有缺口无法定位,AI 不许猜改,人把原文粘贴进卡作答,原文作为人工证据注入回灌回合)。都是人工事实,任何介入档位与月光都不代答。
_Avoid_: 猜改(证据缺口维度不许盲改,这正是举卡的由来);平台分诊/代举(已随 #247 退场,红灯事实投递给 AI 自行判断)

**AI 推荐**:
选项题举卡时 AI 必须指明的倾向项——结构化字段,机械校验必须命中所给选项之一。推荐不代替选择:卡上只标注、不预选,拍板权始终在用户;平台闸的推荐由宿主码表或 AI 提案派生,不是 AI 现场给的。推荐是将来无审批全自动模式的执行依据,所以必有、且必须结构化。

**提前收嘴**:
模型在阶段未走到出口时自行结束回合(输出总结文本、不再调工具)。阶段真相在平台,收嘴不改变阶段;平台以催办续跑把它推回阶段(每回合预算内),连收嘴即转人工。

**出口**:
固定阶段收口的唯一动作,全流程只有两种形态:四个阶段(拉单/拉仓/修复/提交MR)的出口是 complete_stage 自报;两个阶段(问题分析/无单结论)的出口是举卡工具本身——卡即出口,没有 complete_stage 可绕。催办判据 = 未到出口。

**自报推进**:
AI 判断阶段目标已达成,调 complete_stage 宣布收口,平台记录并推进。平台不核实 AI 的工作事实(单读没读完、仓拉没拉齐、码改没改完——那是 AI 的裁量),只保留它做得比 AI 好的两道核验:用户决策卡与 MR 验绿门。

**事实上报**:
report_ut 的语义——UT 并入问题修复阶段(2026-09-02,TDD:先写复现单测再改码),修复过程中 AI 跑完测试自愿上报结构化结果(结论/摘要/日志落点),平台只记账(现场记录可查),不推进阶段、不设门禁。

**MR 验绿门**:
提交 MR 阶段的平台硬卡:AI 申报的 MR 清单必须与会话台账(平台在创建 MR 时自动记的账)一致,且每个 MR 最新推送的流水线全绿,阶段转移才发生。有红当场打回,在跑受理等绿——绿永远是宿主监看器说了算,不是 AI 说了算。

**整体回退**:
检视提交时平台的处置:问题分析及之后的阶段标记重做、回退轮次加一,已申报的 UT/流水线/MR 账作废;分支与 MR 延用——同分支追加修复,不推倒重来。换库验证的机器触发随换库封存移除(ADR-0013);收口后的续聊返工不是回退——重开当前阶段,轮次账不动。

**检视**:
用户针对分析报告提出的修订意见及触发的重跑行为。检视对象只有分析报告,不含代码——代码层面的意见经右栏插话/续聊直接告知 AI(人工修改退役后,问题流不再有第二个人工写口,ADR-0028)。提交检视走整体回退(唯一触发源,人工)。转正后的会话不可再检视。提意见的入口内联在分析报告视图(行尾圈注攒草稿、一键批量提交),无独立检视页签(2026-09-14 拍板,ADR-0025)。
_Avoid_: 审查、复核(与请 AI 复核混称——该入口已随 ADR-0028 退役)、批注(那是需求流检视批注的词;问题流的意见叫检视)

**MR 检视意见**:
检视人在 CodeHub 的 MR 讨论区里对代码提的意见(区别于上一条:那条检视分析报告,这条检视代码)。到达只同步为待判断批注并汇总提醒责任人,AI 永不自行处理——责任人自行答复(答复直达平台讨论,不主张代码已改)、本地闭环、删除,或补充修改要求后批量交办;介入档位不作用于这道确认(ADR-0032)。追踪窗口自 MR 创建至全部合入,合入后不再追、账保留。需求流与问题流同制:两侧的新意见都只入批注等交办,区别只在检视对象(需求文本 vs 代码)。

**追问**:
检视人更新已提交的 MR 检视意见(同一讨论出现新正文)。追问产生新的待判断批注,与普通意见同等待遇;已删除、已答复、已闭环的意见不因追问或重新拉取复活——检视人真有新话就说,新正文即是新意见(ADR-0032)。

**检视意见**:
用户圈在分析报告某一行上的单条修订要求或提问:意见号 + 行号 + 锚定原文 + 自由文本,同行可多条。草稿随时记、可撤;攒批一键提交——AI 必须按意见号逐条回应(改了什么/答了什么),回应落在新版报告开头的「检视意见回应」段(软性章节,不进 submit_analysis 五章节门票)。已提交的意见不再渲染在新版上:归属其提交时所在的冻结版本与回应段,新版是干净纸面(ADR-0025)。
_Avoid_: 批注(需求流的词)、与 MR 检视意见混称(那条检视代码,这条检视分析报告)

**意见号**:
检视意见在单个问题控制台内的序号,自 1 单调递增、永不复用(软删也不回收),展示与 AI 引用统一用它(「意见3」);跨批次连续不重置——第2批接着第1批往下编,保证带号的回应在整个控制台内唯一定位。内部台账另持有 an- 前缀 id,不对外(ADR-0025)。

**分析报告版本**:
平台在检视意见提交时冻结的报告快照(复用 reviews/ 快照账):「初版 / 修订1 / 修订2」,修订N = 第N批检视意见处理后的新版;每批意见处理完必出新版(回应章节必加)。版本是平台的记账行为,AI 只管整份重写 issue-analysis.md。分析报告页签默认进最新版,只有初版时不渲染版本页签条;闸卡「补充意见」等非检视通道的报告重写不出版本,只更新最新版内容(ADR-0025)。
_Avoid_: AI 自落版本文件(issue-analysis.r2.md 之类,已否);「分析报告2」式页签名(已否)

**回合前压缩**:
问题会话续聊回合把话递进在场会话前的上下文压缩(2026-09-11)。唯一安全位在 resumeTurnBody:挂起通道(问题卡原地续跑)与重启重建(新上下文)结构性不压。两条触发路:①分析→修复边界(analysis_confirm 确认)必压一次,不受阈值管辖;②事件量阈值(管理页旋钮 issue_compact_every_events / 部署旗 --issue-compact-every-events,部署缺省 400≈一扇网关窗的事件量——issue-64 实测约 420 条撑满一窗,#285 拍板零配置即开;显式 0=关)——events.jsonl 增量是上下文增长的诚实代理(与需求侧 compactEveryEvents 同一纪律)。两路都 fail-open:压不动回合照走。边界的报告指针(路径+修改方案要点)钉进确认推进通知词走必达通道——pi 的手动压缩在单回合历史上不带自定义锚(split-turn 路),指针不能指望摘要。
_Avoid_: 把超限自愈(turnWithOverflowRepair,上下文撑爆后的补救)说成回合前压缩(那是兜底,这是节奏);超限自愈预算"同一场连续超限只补一次、翻篇(补救重试不再超限)即归还"(2026-09-16 #285,issue-64 二爆冤死复盘),不是整条会话只救一次

**现场回收**:
磁盘治理(2026-09-11 拍板):终态(取消/归档)问题单的 `repo/` 代码现场由每日清扫器整树删除,取消/归档当场也回收;idle 单的构建产物(target/build/node_modules/depend)在 mtime 冷却 ≥48h 后单独回收,源码与 .git 保留。**failed 全豁免**(可恢复态,手动转取消是其出口);suspended 等转正,不碰。过程记录(issue.json/事件账/分析报告/拉取日志/登记截图)永不回收——历史会话照常可看。双旋钮独立:issue_repo_reclaim(整仓开/关)、issue_build_products_cooldown_hours(产物冷却期,0=关)。回收过的单子返工时,回合提示词带"首次编译为全量"预告,不走预热。
_Avoid_: 说成"删除问题单"(只删代码现场,单据与过程记录全在);给 failed 设自动回收(可恢复态,用户明确豁免)

**月光模式**:
个人设置「人工介入程度」(**需求交付**)的过程轴(moonlight):开着时需求流的过程人工节点由系统代答放行——代答认得出的确认类选项(认不出不猜),并按推荐项整卡代答 Agent 自举的纯选项题问答卡(开放题、混卡、检视回合的卡永不,ADR-0006);每处现读现判,随时开改,已挂起的卡不追溯代答。问用户事实的闸(环境配置、验证结果)与 MR 人工合入永不代答。2026-09-09 起月光不再作用于问题流——问题侧改用介入档位(ADR-0019)。
_Avoid_: 用月光描述问题侧行为(问题侧的词是介入档位)

**推送前过目**:
push 分支出门前给人看变更清单的闸:push_branch 被平台举起「推送确认」卡(带服务端现查仓库生成的变更摘要),确认产一次性令牌放行一次推送,下次推送重新过目;答「暂不推送」不产令牌,AI 按意见调整;月光不代它——过目是用户显式开启的意志,更具体的意志赢(ADR-0009)。多仓交付逐仓确认、推送串行(2026-09-10 issue-14 收口):push_branch 在途互斥(同毫秒并发调用一个放行一个打回),已举卡在等作答时连环重调快速打回、不重举顶掉旧卡。需求交付侧是个人设置的独立交付轴(缺省即开);问题侧 2026-09-09 起没有独立开关,并入介入档位定义——「全程把控」过目,「全自动/仅分析报告」直推(ADR-0019)。
_Avoid_: MR 合入确认(合入永远人工,与过目是两道门)、给问题侧单独配过目开关(轴已并档)

**介入档位**:
个人设置问题处理节的唯一旋钮,三档,缺省「仅分析报告」,现读现判(ADR-0019)。**全自动**:人工零介入——不举决策卡、不问环境(环境缺口最多在分析报告里提一嘴),高置信非问题自动关单+通知,直推,一路跑到 MR 变绿(或办不了停下)才提醒人;**仅分析报告**(默认):唯一停靠点是分析报告的确认卡——人检视提意见(报告圈注或对话),AI 闭环重出,直到人点下确认,点确认即视为审查通过;其余与全自动同;**全程把控**:原问题流默认行为——提示词驱动的主动对齐(证据不足主动问、关键节点对齐)、结论确认、环境闸、推送过目全保留。流水线红灯的人工事实闸(不可修/证据回灌)三档都保留:那不是询问,是只有人做得到的事。
_Avoid_: 月光/推送前过目(问题侧的两轴叫法已退役)、批注(意见叫检视,圈注只是通道之一,见「检视」条)

**模块预绑**:
单号→业务模块的人工绑定(DTS 列表每行的模块下拉,选即存,团队共享人人可写):发起时会话静默携带预绑模块与它的仓,AI 跳过检索识别直接拉仓。人工选定的模块对 AI 锁死——bind_module 工具级拒绝改绑,发现不符只能问人(ADR-0010);服务端按单名的自动匹配仍是猜测,不锁。改绑只影响下一次发起,进行中会话不追溯。临时方案:上游 DTS 原生提供模块字段后整体退役。
_Avoid_: AI 识别绑定的模块(那是运行时检索,与预绑相对)、模块归属审核(人人可写,无审批)

**版本组**:
DTS 单据版本剥掉尾部 B 版构建号后的版本段(如 V100R025C10SPC010B009 → V100R025C10SPC010)。DTS 列表的版本过滤按版本组勾选——勾一个组,组内全部 B 版单据一并命中;列表的版本列展示含构建号的完整版本,过滤只认版本组(用户口中"只按大版本过滤,不看小版本")。默认勾选并列最高 R/C 的全部组。
_Avoid_: 大版本/小版本(口语对照:大版本≈版本组,小版本≈B 版构建号;正式词用版本组)、完整版本过滤(逐构建号勾选,2026-08-29 废除)

**已发起**:
一张 DTS 单在当前登录用户名下存在进行中的问题会话(未归档、未取消、未失败)。判定与发起拦截同尺:单子显示"已发起"当且仅当此刻再点发起会被拦;名下只有终态(归档/取消/失败)会话的单不算已发起,可再次发起。DTS 列表的「发起状态」列按它展示,列表默认滤掉已发起的单。
_Avoid_: 启动过分析(口语对照,正式词用已发起)、曾发起过(终态旧会话不算已发起)

**一次修复成功率**(2026-09-17 定,替代「一次通过率」):
度量问题修复轴的指标:完成交付的会话中,环境验证一次过的占比。分母=完成交付——有单且全部 MR 合入自动归档(结论 delivered)的会话(ADR-0034;存量全合入的手动归档同形计入);分子=其中从未答过「验证发现问题」的会话(自动归档时未答卡=验证通过)。取消、失败、非问题收口、挂起一律不进分母。全平台全量累计,呈现于团队问题页签。
_Avoid_: 一次通过率(2026-09-17 退役的名字,验收语义并入修复轴)、准确率(没有 ground truth 对照,测的是用户验收事实)、把取消/误报算进分母(它们不是完成交付)

**一次定位成功率**(2026-09-17 定):
度量问题定位轴的指标:完成交付的会话中,分析报告一版过的占比。分母同「一次修复成功率」(完成交付);分子=其中分析报告版本数 ≤1 的会话(ADR-0035 起版本只随修改型检视增长,回复型不计)。
_Avoid_: 以检视批次数代替版本数(分诊后两者脱钩)、把没完成交付的会话算进定位评价(定位只对走到交付的会话结算)

**现场记录**:
一个问题会话的全部执行事实记录:事件流(工具调用及其输入输出、AI 发言、用户指令与决策)加会话台账(单号、仓、阶段推进、结论)。它是复盘与问题定位的原始凭证——现场页签是它的直播视图,导出文件是它的快照。

**现场直播**:
现场记录的实时视图(执行事件边发生边推送),问题工作台「对话现场」标签的内容体;带筛选(全部/消息/工具/异常)。与「过程问答」相对:直播是正在发生,问答是回头看——同一条现场记录的两个时刻,不是两份数据。(「过程问答」页签已于 2026-09-14 随 ADR-0025 退役,现场记录的人读面由直播独承;对照关系留档考古。)

**查看模式**:
非归属人打开他人问题会话工作台的形态:全部现场对登录开发者可见(概要、时间线、材料、事件流),零操作——续聊、作答、取消、归档(仅无单结论后)、绑单、检视、材料修改等一切写动作仍归归属人。与需求流"人人可看、归属人操作"同一约定;管理员的只读是既有角色边界,不是这个词条的延伸。
_Avoid_: 协作模式(没有第二写入者)、只读会话(会话本身照常在跑,只是看的人不能动)

**过程问答**:
现场记录面向复盘的投影:只呈现 agent 的提问(举卡)、用户对卡与平台闸的答复、用户的主动插话;agent 的过程性发言不进入。只追加、不回写——agent 修订分析报告也不改写已有问答,新事实以新回合出现。(本词条已于 2026-09-14 随 ADR-0025 退役:过程问答页签、projectDialogue 投影接口整体删除,现场记录的人读面归「现场直播」;词条留档考古,不再指活代码。)
_Avoid_: 对话记录、聊天记录(它不是完整对话);把过程问答当现存页签/接口(已于 2026-09-14 随 ADR-0025 退役)

**通用定位 skill**:
团队货架发布的、未限定作用域的 engineering 知识资产,装载为只读快照 `.mae-flow-work/host-skills`,由 AI 按 skill 渐进式发现建索引自取(ADR-0015 补全发现面)。定位方法论的第一顺位供给;货架无匹配时退到分析工作流(issue-analysis)的自力定位路径——取用是替换式的,不叠加。
_Avoid_: 把仓内技能称作通用定位 skill(它们只供领域知识,不定流程)

**业务问题 skill**:
业务代码仓随仓维护的问题定位知识,约定放在仓根目录 .cac/skills/ 的标准 skill 目录形态(与需求侧仓内能力发现同一约定),拉仓后按需阅读。它供的是领域事实与排障方法;流程、报告格式、停机节奏由问题分析工作流的平台契约决定,不由它约定。

**业务知识**:
问题分析消费的领域事实库(ADR-0012),三个来源一张地图:①团队资产库按绑定模块定格的已发布知识(只读投影 .mae-flow-work/business-modules/,台账防版本漂移,条目带 title/summary/when_to_use 检索意图,进 analyze 时自动注入);②仓内业务知识(已拉仓 docs/ 目录树)——平台不扫不注入,发现权完全归仓:仓根 AGENTS.md 用标准句声明"docs/ 维护业务知识"即高置信,无声明的仓 AI 可参考但置信度中等,以代码为准;③知识仓(ADR-0033,配置中心管理员指定的全局领域知识仓,开工整仓预拉为只读参考件)。三源正文一律按需 Read;引用业务事实写文件路径进证据链。
_Avoid_: 业务模块知识(那是需求侧对同一资产库的消费形态名)、业务问题 skill(skill 供排障方法论,业务知识供领域事实,互补不互斥)

**业务知识树**:
业务仓 docs/ 下用文件路由组织的分层知识(总览→仓内模块,纯仓内资产):目录与文件名即索引,发现靠 AGENTS.md 标准句声明,不靠平台扫描;平台对树的内部组织无感知、无约定,按目录+文件名治理是仓主人的责任。
_Avoid_: 业务知识(那是三源一张地图的整体)、模块知识(跨仓粘合层,存放处不同)

**模块总览**:
跨仓业务模块在资产库发布的一篇总览知识:多仓同模块的粘合层,模块 Owner 维护,analyze 随模块定格。单仓模块不设——仓概览即总览。
_Avoid_: 业务总览(已拆分为模块总览与仓概览,勿再单用)、仓概览(那是仓根一层)

**仓概览**:
业务仓根 AGENTS.md/CLAUDE.md 承载的仓内概况:平台逐仓收集契约文件注入系统提示词(候选序与 SDK 一致取一份,单仓超 16K 字符截断),模型要全文按需 Read。仓主人用标准句声明 docs/ 业务知识(见业务知识词条)即视为高置信维护;仓没有契约文件时,docs/ 内容置信度中等,AI 可参考但以代码为准。
_Avoid_: 平台不加载仓根文件(那是 #132 之前的旧口径——SDK 祖先发现从会话根出发永远望不到仓根,平台代收)、模块总览

**skill 渐进式发现**:
问题定位 skill 的统一取用方式(ADR-0014/0015,2026-09-03),两翼同规:业务仓 skill 逐仓列 `.cac/skills`(与 .agents/skills 补位)建索引,货架通用 skill 列只读快照 `.mae-flow-work/host-skills` 建索引;拿索引与当前问题对相关性,相关才读正文——全程 AI 自主,不分介入档,平台不举卡不圈选。analyze 入口的扫描账进转移账(现场可查当时有哪些可用)。描述没命中是维护者该修的描述,不是运行时人工圈选该补的课(2026-09-02 的圈选闸由此拆除,ADR-0011 封存)。
_Avoid_: skill 加载(平台从不装载业务仓 skill,Agent 始终自己读)、白名单(封闭集,从未存在)、人工圈选(已退役的中间形态)

**轻量路径**:
问题分析工作流为简单问题(已知问题、一眼可定位的配置错误)预留的最短路径:工单信息加业务问题 skill 直接命中即达结论。走不走由 agent 判断;报告出简版,但要素不缺。

**分析报告**:
问题分析阶段的核心交付物,落工作区根(issue-analysis.md):首行一句话总结(串联 现象→根因→方案,只读这行就能拍板),正文五节——问题现象/问题根因/修改方案/证据链/置信度。证据链每条一行=事实+出处指针(原文不贴,只收与结论相关的证据:支撑为主,反证至少一条或声明"未见反证",上限5条)——它的读者是下一棒的 AI,证明"证据真被找过";修改方案是 fix 阶段的执行承诺,不是建议。提交的是收敛后的结论版;续聊继承、无单转正、复盘检视都认它。修订版开头另带「检视意见回应」段,按意见号逐条答复(软性章节,见「分析报告版本」)。
_Avoid_: 无一句话总结的开头;日志整段摘录、过程回放式长文、只拣顺证据的证据链(2026-09-03 定型:现象-根因-方案三段式解禁为章节结构——2026-08-31 曾避它,病根其实在长度不在结构;「下一步建议」并入修改方案,从建议升格为承诺)

**提示词文案**:
平台直接说给 AI 听的自然语言(契约条文、平台通知、工具回执、阶段引导语)。存 `assets/issue-prompts/` 静态 md,服务加载时挂载,锚点是代码协议(ADR-0016)。与阶段注册表分家:文案随便改,结构留在代码——阶段简报的**自由引导语**也是文案(ADR-0017 起住 briefs.md,如"进 analyze 先调 grilling 对齐"),而**契约句**(出口条件/可用工具清单)由注册表代码拼在引导语后面,引导层与门禁层同源不可拆。
 _Avoid_: 提示词工程(那含工具 schema 与用户卡面,都不在此列)、模板引擎(只有 {{变量}} 纯替换)、在 briefs.md 里复述出口条件与工具清单(契约句只住代码,复述=第二真相源)

**调试形态**:
问题流面向本机开发与演示的部署形态,单一开关进:外部触点全部落到本地仿真——DTS 单据读调试数据文件,交付平台是进程内恒绿假件(流水线永远通过、MR 永不合入),代码远端是本机仓的 bare 镜像(克隆与推送都不碰源仓),网管日志由罐头目录顶替,业务模块与网管环境随启动自动种好。调试形态走的问题会话与正式流程同一套阶段、工具与门禁,只有数据来源是假的。
_Avoid_: 把调试形态的恒绿/永不合入当真实交付事实;在正式部署开启;为调试形态另起第二套流程语义

 ## Workflow

**UI 轨道纪律**(2026-09-11 拍板:shadcn base-nova 皮融进,模型写 UI 不许自由发挥):
`web/` 新轨界面写 UI 只有三条路,没有第四条:
1. **只准用 `web/src/components/ui/` 现有包装层**(button/dialog/select/popover/…);缺组件先 `npx shadcn add`(style 已是 base-nova,拉下来的就是 Base UI 版),复制后按需改,不许手搓同功能件。
2. **颜色/圆角/字号只从令牌工具类拼**(bg-primary、text-muted-foreground、rounded-lg 这类)——禁 `bg-[#hex]` 式硬编码、禁在页面里自造组件皮;tokens.css 经 tailwind.css @theme 桥出的语义类是唯一色彩词汇。
3. **交互/弹层一律走 ui/ 包装层**:焦点圈、键盘导航、进出场动画都已在皮里(nova 类 + tw-animate-css),页面只管排版(flex/gap/padding),不做视觉设计。
存量 legacy 轨(手写 CSS、nav-item 家族)不强制改写,但新页面禁再入那条轨。card/table 是有意本地化的扁平变体(见文件头注释),是其作者拍板的例外,不照 nova 重绘。
_Avoid_: 在 tw-root 轨道裸写 `<button>`/弹层、给同一页面混用两条轨道的类名、改 ui/ 包装层里 nova 的类形(要改先在 project.md 记录理由)

**pr**:
用户说「pr」「pr合并」「pr 合入 main」的既定语义:推送当前分支后建 PR 并直接合入 main,一气呵成,不必再等第二次确认。「pr 合并到 main」与「pr合并」同义;只建不合说「建 PR 不合」。
_Avoid_: 建 PR 后停下等拍板(那是「建 PR 不合」的显式要求)、本地直接 push main(永远走 PR)

**并行提交纪律**(2026-09-10 事故立规:b1e9f46 误剔并行会话已提交实现 421 行):
同一工作区多会话并行时,提交前必须 `git diff --cached` 逐 hunk 核对暂存内容——pathspec 只圈得住文件、圈不住文件里的外来改动;双方都在写的共享文件禁整文件提交,外来 hunk 一律留在工作区归其作者;看到 git status 里的陌生改动,先当作并行会话的在途工作对待,不得覆盖、不得顺手提交。回退/重建共享文件前,先 `git log <file>` 确认没有并行会话落在自己基线之后的提交。
_Avoid_: 整文件 pathspec 提交共享文件、以提交时点的工作区内容为准做"干净重建"(可能吞掉并行的已提交代码)

**服务验证纪律**(2026-09-11 立规:8787 两度"以为部署了新版"实为旧构建;2026-09-14 补自动化条款):
8787 本地实例永远从主仓(/home/ning/code/mae-flow-cloud)启动(`npm exec tsx src/serve.ts`,缺省端口 8787;前端静态根是 `web/dist`,改前端必须 `cd web && npm run build`,根目录没有 build 脚本)。改动落地的方式:本地分支改完先合入本地 dev(并行提交纪律同样适用),主仓快进到最新 dev 后 `npm install && cd web && npm run build`,再从主仓重启 8787。
**部署链是改动收口的一部分,自动执行**(2026-09-14 拍板):改完提交即构建+重启 8787,不等用户问——用户看 8787 是验收的默认位置,「8787 咋看不到」就等于部署没做。重启后必须经服务端口验证真实渲染的新 UI 特征(如 curl 产物 chunk 找新文案),不凭"提交了就应该有"。
_Avoid_: 从临时 worktree 长期跑服务(下次重启没人知道它从哪起的)、只凭构建产物文件名或 bundle hash 断言"新版已生效"、把部署留给用户下一步催(收口即部署)。

**测试纪律**(2026-09-09 拍板:8 分钟的文案改动花 30 分钟,其中 20 分钟耗在与己无关的红测试归因;同日二次拍板:永远不做全量回归,pr 前也不做;2026-09-13 三次拍板:全量上 CI 非阻塞报告,迭代速度优先):
测试量与改动的行为面对齐——能不跑就不跑,能少跑就少跑。按改动分档:
- **纯文案/注释/字符串**(无逻辑面):类型检查 + 直接锚定该文案的测试,到此为止,不跑套件。
- **局部逻辑**:点名跑受影响套件与直接消费者(`--test-name-pattern` 优先于整文件、整目录)。
- **永远不跑全量**:任何时点都不在本地做全量回归,pr 前也不例外——全量的成本永远压过它的增量信息;没被点名测试覆盖到的回归,事后点名补测修掉,不靠全量兜底。全量是 CI 的事(2026-09-13 起):推送触发 CI,本地迭代只守秒级 gate,不为等全量停下;全量报告非阻塞,存量低质量红清零前不许它挡任何人——**原则上迭代速度越快越好,慢而弱的覆盖没有阻塞资格**。
- **遇红先对基线,再谈归因**:第一动是确认"我改之前它红不红"(stash/纯净检出后**点名跑该用例**),一次带证据记账;已知红(并行会话中间态、基线固有)记下即跳过——不重跑、不跑对照矩阵、不替并行会话验证。归因永远单用例点名。
_Avoid_: 全量/准全量命令(npm test、test:fast)作为验证手段——用户点名要求时除外、改一句文案跑套件、为"确认与己无关"反复重跑同一红、三向对照矩阵(基线一次即够)、无证据就默认不是自己的锅(b1e9f46 的教训对测试同样成立)
