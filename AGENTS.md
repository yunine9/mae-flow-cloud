# 仓库维护指引

排查或修复 mae-flow 内核及 mae-flow-cloud 宿主流程机制引发的问题时，必须先读取并应用 [kernel-simplification 技能](skills/kernel-simplification/SKILL.md)，再设计和实施修改。典型场景包括重复审批、流程阻塞、反复唤醒，以及收据、指纹或 SHA 判定引发的循环。

处理顺序是：横向排查同源场景 → 优先评估删除机制 → 不能删除再简化 → 工作方法用提示词约束，必要的 Cloud 专属逻辑收敛到 TS。具体边界和验收方式以技能为准。

普通业务功能或 UI 修改无需仅因位于本仓就套用该技能；本指引不授权超出用户当前任务的内核重构。
