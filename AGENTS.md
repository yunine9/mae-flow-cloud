# 仓库维护指引

内网问题统一通过本仓 GitHub Issue 跟踪。用户提供 Issue 编号时，先读取正文及评论，以现场证据核对当前代码，再定位和修复；不要仅凭报告中的推测直接修改。

核心取舍：流程不等于质量，却直接影响闭环速度；当流程不能显著提升质量时，应优先为速度让步。不要把完成流程当作质量证明。

排查或修复 mae-flow 内核及 mae-flow-cloud 宿主流程机制中**需求交付流程**（taskService 及需求侧链路）的问题时，必须先读取并应用 [kernel-simplification 技能](skills/kernel-simplification/SKILL.md)，再设计和实施修改。典型场景包括重复审批、流程阻塞、反复唤醒，以及收据、指纹或 SHA 判定引发的循环。问题单处理流程（issueFlow）禁止读取或套用该技能。

处理顺序是：横向排查同源场景 → 优先评估删除机制 → 不能删除再简化 → 工作方法用提示词约束，必要的 Cloud 专属逻辑收敛到 TS。具体边界和验收方式以技能为准。

普通业务功能或 UI 修改无需仅因位于本仓就套用该技能；本指引不授权超出用户当前任务的内核重构。

巡检生产需求任务、分析真实任务耗时或配置内网 PI 定时巡检时，读取 [task-patrol 技能](skills/task-patrol/SKILL.md)。巡检只读生产，报告与调度运行在内网本机；它不授权自动修复、推进任务或部署。

## 文档与沟通的用词纪律

写进仓库的文档、注释、提示词必须用人话，禁止使用未在 CONTEXT.md 定义的内部黑话（如「点火」「陈灯」「派修」「结算」「投递」）——AI 读什么就学什么说话，提示词里的黑话会原样漏到用户面前。CONTEXT.md 已定义的领域词（闸、回合、收口等）可以用，但文档中首次出现要带日常语言解释。与用户对话同样禁止抛出未解释的内部用语。

## 界面开发与验收范围

产品面向 Windows 浏览器，按 16:9 桌面窗口设计和验收（默认 1920×1080；必要时检查 1366×768 等常见桌面尺寸）。除非用户本次明确要求，否则不要做手机或平板适配，不要测试移动端断点、模拟手机设备或为移动端扩大改动范围，避免无关的时间和 Token 消耗。

本机预览必须管理进程生命周期：临时验证结束后停止所启动的服务及子进程；为用户保留预览时记录端口、PID 和数据目录，后续替换预览时停止旧实例。不要把后台服务长期挂在工具会话的 stdout/stderr 管道上；后台输出重定向到可控日志，日志必须限制大小。删除预览数据目录前先停对应进程，不能只删目录留下写入者。

## 测试验证范围

默认不跑全量测试套件（`npm test` / `npm run test:full`）。验证按改动域定向：

- 测试范围用 grep 枚举被改端点/组件/服务的调用方，只跑命中的测试文件，配合 typecheck；定向跑绿即视为验证完成。
- 全量套件与聚合 gate（`npm run gate`）仅在用户点名要求时跑；已单项跑绿的检查不重跑聚合 gate。
- 遇到与本次改动无关的存量测试失败：确认无关后按失败所在域报告即止，不追查、不做 stash 基线对照重跑。分支本身是红的时，全量红灯没有信息量。
