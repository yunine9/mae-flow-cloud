# 02: 意见注入与自动修复(修得掉)

**What to build:** 门禁红(存在未解决讨论)时,新检视意见以平台通知注入 AI——不举卡、不打断(并行哲学,2026-09-08 拍板),汇总"文件:行号 + 正文"清单喂给上下文;AI 按问题流现成返工路自主修复:同分支追加修改、重推、重建 MR、重新申报验绿。检视人解决讨论后,意见记录统一闭环标注。关闭自动修(repair 预算=0)时:不派修,意见保持可见并如实标注"待人工处理"。

> 修订(2026-09-08 评审):闭环语义按 **discussion id 消失**判定(适配层
> discussions 列表只回未解决项,某 id 不再出现=检视人已解决),不走
> `resolve_discussion_passed` 门禁——少一个部署前置,且粒度更细(逐条
> 闭环)。门禁如需"整体确认"语义可后补。

**Blocked by:** 01-discover-discussions.md(已完成)

**Status:** done

- [x] 新意见 → 平台回合通知注入(会话空闲才点火;忙时挂起等下一拍,内容恒为当前全部 open 意见)
- [x] 修复走现成基建:同分支 push_branch → create_mr(重建)→ complete_stage 重新申报,零新通道(e2e 实证)
- [x] 通知注入不占举卡:意见在反馈面板可见,不新增人工确认点
- [x] 讨论解决 → 意见闭环标注(id 消失判定);此后同 id 不再注入
- [x] 自动修关闭(预算=0)时:标 needs_human("repair_rounds=0"),不注入模型
- [x] 与既有机制不冲突:注入只在会话空闲时点火;收口/归档即停
- [x] 测试:意见到达 → 注入 → 修复重推 → 重新申报 → 解决 → 闭环,全链路(假件驱动)+ 预算=0 分支

**边界:** 检视修复与流水线红灯修复共用回合与并发额度,不另设预算旋钮;若实测互相挤兑再议拆分。
