"""交付现场面板:只读展示层。

定位(改这里之前先读):

- **只读**。本包不写任何流程状态。阶段真相只在 .mae-flow.json,写入只走 done。
  快照读状态、读文件、读 git,除此之外什么都不做。
- **可缺席**。没有面板,流程一模一样能跑完。任何"只有面板上才能确认"的环节
  都是设计错误——面板是增强,不是依赖。
- **软失败**。出口自己出错绝不变成新卡点:不抛栈、不返回非零,把问题写进
  快照的 warnings,其余字段照给。
- **不触发重活**。只读文件与 git(带 timeout),禁止调 CodeCheck/npm/编译,
  禁止安装任何东西——看现场不能改变现场。
"""
