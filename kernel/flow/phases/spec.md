## Objective
Define WHAT observable outcome is required and what is out of scope.

## Inspect
Read the request, current behavior, constraints, acceptance boundaries, and
unresolved product risks. Run the lightweight, read-only Grill critic exactly once.

## Stop for the user
Stop for genuine product ambiguity or approval of the observable scope.
Stop for a real reviewer tradeoff. CLEAR continues without a user stop.

## Outputs
In the local Spec acceptance section, design one-line cases with stable IDs and bold condition, action, and expected-result labels. State shared test setup and external dependency boundaries once. Refer to the global Story for module responsibilities and cross-module scenarios. Keep planned results separate from execution evidence; disclose missing environments without adding a gate.

Produce the reviewed specification and record remaining risks or decisions in
natural language. Record critic failure without automatic retry.

## Next
Proceed to Story. The next meaningful action is to confirm the reviewed WHAT.
