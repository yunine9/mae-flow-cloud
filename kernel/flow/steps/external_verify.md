分支已经推送，当前由宿主触发权威流水线并逐项核对：

- 编译（COMPILE）
- 单元测试运行（UT；测试编写已在前序步骤完成）
- CodeCheck

本步是宿主等待点。不要执行本地编译、UT 或 CodeCheck，不要调用 `done`、
`skip`、`goto`，也不要向用户发确认卡。结束当前回合即可。

若流水线失败，宿主会启动一个轻量专职修复 Agent：读取失败材料、定位并一次
修全，集中提交和推送，再以新 SHA 重跑流水线；不回退人工 Diff 检视，也不重走
前面的完整质量流程。只有三项结果都绑定当前最终 SHA 且通过，内核才自动进入终态。
