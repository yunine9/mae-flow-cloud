用户已经检视当前完整未提交增量。先用 `git status --short` 核对文件，只精确 `git add -- <文件...>`，
再按 `[单号][feat|fix]描述` 创建一次实现提交。禁止 `git add .`、`git add -A`、amend 或夹带过程文件。
提交成功后执行 `done` 进入对应质量链。后续质量改动使用统一检视和提交节点，不允许质量 Agent 自行提交。
