**配置确认卡(一卡合一)已收集过本项时:直接 done --choice yes|no,禁止重复提问**;以下仅在预答缺失时执行。
用 AskUserQuestion 询问是否展开需求质询,两个选项:
- 做质询(文档较新/跨模块/**需求材料单薄**时建议):按 8 个维度审计缺口,逐题确认没写清的决策并落盘
- 跳过(文档成熟或赶时间)
推荐逻辑:需求只有一句话口述/寥寥数行时,推荐项=**做质询**并说明理由——材料越薄缺口越多,
质询是把一句话需求补全成可实现规格的主要手段,跳过等于让 AI 替用户脑补需求。
(工具不可用才纯文本结束回复等回答。)
拿到按钮选择后直接 done --choice yes|no，不再要求用户输入确认句。
