# GRILL-PREP-{单号}

8 维备课工作表:逐维扫描 SE 文档与相关代码后填写,是 Grill 提问前的落盘产物(hook 校验章节结构)。
**每维必须产出实质结论,禁止快速划过**——每维二选一并删除未选行:
- **有缺口** → 至少 1 个候选题,四要素齐全:缺口位置 | 代码佐证 | 推荐答案 | 实现影响;
- **不适用** → 必须写具体依据(定位到代码/文档;"无关""不涉及"不是依据)。
填写 = 把所有 `{{…}}` 占位整体替换为实际内容,残留 `{{` 或"待填"会被 hook 拦(逼你真扫过每一维)。

## 1 状态机完备性

- 扫描:{{状态×事件矩阵是否完备;非法迁移、未定义事件时的行为}}
- 结论(二选一删一):有缺口 → 候选题:{{缺口位置|代码佐证|推荐答案|实现影响}} / 不适用 → 依据:{{具体到代码/文档}}

## 2 边界值

- 扫描:{{0/最大/空集/重复/超长等边界输入的行为}}
- 结论(二选一删一):有缺口 → 候选题:{{缺口位置|代码佐证|推荐答案|实现影响}} / 不适用 → 依据:{{具体到代码/文档}}

## 3 并发时序

- 扫描:{{同时修改窗口、互斥、重入、乱序到达}}
- 结论(二选一删一):有缺口 → 候选题:{{缺口位置|代码佐证|推荐答案|实现影响}} / 不适用 → 依据:{{具体到代码/文档}}

## 4 失败路径与残留清理

- 扫描:{{各步失败后的状态与重试语义、残留数据清理}}
- 结论(二选一删一):有缺口 → 候选题:{{缺口位置|代码佐证|推荐答案|实现影响}} / 不适用 → 依据:{{具体到代码/文档}}

## 5 数据一致性

- 扫描:{{事务边界、部分成功、回滚}}
- 结论(二选一删一):有缺口 → 候选题:{{缺口位置|代码佐证|推荐答案|实现影响}} / 不适用 → 依据:{{具体到代码/文档}}

## 6 存量升级兼容

- 扫描:{{老数据/老版本共存期行为、灰度}}
- 结论(二选一删一):有缺口 → 候选题:{{缺口位置|代码佐证|推荐答案|实现影响}} / 不适用 → 依据:{{具体到代码/文档}}

## 7 规格性能

- 扫描:{{规模上限、耗时预算、同步/异步}}
- 结论(二选一删一):有缺口 → 候选题:{{缺口位置|代码佐证|推荐答案|实现影响}} / 不适用 → 依据:{{具体到代码/文档}}

## 8 可观测

- 扫描:{{日志/告警/用户提示;失败时如何被发现}}
- 结论(二选一删一):有缺口 → 候选题:{{缺口位置|代码佐证|推荐答案|实现影响}} / 不适用 → 依据:{{具体到代码/文档}}

## 9 结论汇总

- 候选题按阻塞性排序:{{逐条列出跨维度的候选题清单}}
- 留给设计阶段(技术分歧):{{清单,无则写"无"}}
