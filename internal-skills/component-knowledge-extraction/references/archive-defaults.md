# 默认归档位置

平台读取以下配置，在「入库与更新」带出默认目录和文件名；目标仓默认使用本次研究的第一个组件仓和分支，归档时可以修改。管理员可在 Skill 编辑器修改配置，已准备或已提交的归档不随默认值变化。

```json
{
  "component_directory": "docs/components",
  "component_filename": "component-guide.md"
}
```

萃取阶段只生成联合文档与能力章节，不要求选择归档位置。归档时平台把勾选章节组成提交稿，比较目标目录的已有文档，再由用户处理需要合并的内容。
