/** 交付拆分的方法指导；分析任务与开发中提议拆分共用，不新增调度门禁。 */
export const DELIVERY_SPLIT_GUIDANCE = `拆分目标：缩短整项需求的交付时间，减少下游等待；不是让子任务数量最多或粒度最细。遵循高内聚、低依赖、可验收，并尊重用户明确指定的交付边界。

1. 按完整交付拆，不按文件数、函数数或机械的技术步骤拆。每个单元说清解决什么问题、交付什么、如何验证；相关实现与测试放在一起；后续每个功能模块包含完整接口接入、业务逻辑和测试，按各自职责验收。小任务能够快速完成时不硬拆，拆分带来的并行和检视收益应大于额外分支、MR、构建与集成成本。

2. 优先识别会挡住多个下游的“大前置”。逐个追问下游实际需要它的哪一部分，能否把这部分提早做成最小完整交付。不要把公共能力、所有业务实现和全部测试都塞进一个上游任务，让下游等待整个大 MR。前置交付必须与现有行为兼容，并有与其职责相符的验证；空函数、假实现或仅能编译的骨架，不能冒充下游需要的真实能力。

3. 区分接口约定与真实代码依赖。接口、数据语义和错误行为明确后，能够独立开发的部分可以并行；使用替身时明确哪些尚未经过真实联调。只有确实需要上游交付后才能推进的关系，才写成执行依赖。依赖理由说明“等上游交付什么、为什么当前不能继续”，不要因为有调用关系就要求等待整个上游任务。

4. 保持分支、MR 和合入方式简单。按仓组织交付，每个交付单元应形成边界清楚的 MR；对确实需要上游代码的工作，优先让必要的小前置先合入目标分支；能够独立推进的部分不必一起等待。不要自行设计任务分支套任务分支、夹带上游未合入提交或级联变基。跨仓交付说明接口或依赖产物的取得方式、兼容要求及必要的合入或发布顺序，不把上游 MR 合入等同于下游运行环境已经具备能力。同仓不等于必须串行，不要仅因同仓就编造执行依赖。前期分析就确定可并行单元与真实前置，平台按确认后的依赖建单，不额外添加同仓串行等待。“提前开始”仅是执行期调整的兜底。谨慎核对改动范围、共享接口及核心逻辑：边界清楚且不依赖未落地代码时并行；集中修改同一核心逻辑、接口不稳定或证据不足时先解决不确定性，必要时保留有具体理由的依赖或合并单元。已有公共基础就直接并行，不强行新建框架任务。同仓并行任务需使用不同 AR，避免分支冲突；保留真实前置条件，实际启动取决于剩余依赖和空闲执行名额。

5. 如果几个部分必须频繁一起修改、接口反复变化，或拆开后无法给出有意义的验证结果，就合并到同一交付单元；不能安全切开的真实依赖可以保留等待，并说明原因。交付任务不必与 Agent 内部分工一一对应，不为每份分析或编码工作都创建独立分支和 MR。

6. 在现有 Story 的模块职责、接口、验收和依赖说明中写清上述边界；Archify 架构图表达各模块做什么、产出什么、实际依赖什么，与 Story 保持一致。架构模块与交付任务不必一一对应，图上的调用关系不自动成为任务的串行等待；建单投影只提取交付单元与必要的执行依赖。只使用现有产物和字段，不新增审批卡、流程附录或检查清单门禁。

示例：A 要实现公共报告能力，B、C 分别接入两种业务。若 B、C 只需要稳定的报告地址查询能力，优先把兼容的查询接口及必要实现、测试拆成小前置；前置合入且依赖产物可用后，A 的剩余功能与 B、C 中无其他依赖的工作并行推进，各自使用独立 AR、分支和 MR；后合入者同步目标分支并验证受影响改动。不要等 A 整个报告系统做完，也不要把“声明接口、写函数、补测试”机械拆成三个交付任务。若查询能力离不开仍未确定的报告语义，则先解决该不确定性，不伪造可并行条件。

提交拆分建议前自问：每个单元能否独立说明交付与验收？下游是否只等它真正需要的部分？拆开后是否减少等待和协调，而不是把成本转移到分支、MR 和集成？这用于改进方案，不是新增人工确认步骤。`;
