/** 对外是面向组件使用方的受支持入口，不等于语言可见性或面向公网。 */
export const COMPONENT_API_BOUNDARY = [
  "研究重点是组件向使用方提供的能力（包括公司内部其他团队），允许扩大分析相关能力和实现，不必等发布证据齐全才开始研究。不是 public 的都能用：public、可导入、可链接或其他仓已经调用，只能说明技术上可访问或实际被使用，不能单独证明这是受支持的对外接口。多分析可以，不能把待核实候选或内部实现说成已确认的推荐 API。",
  "重点关注 interface/、idl/，结合 interfaces/、include/、api/、SDK 门面、文档及 POM 联合分析，不规定先目录后 POM 或先 POM 后目录的固定顺序。检查配置范围内是否有 sdk/pom.xml；存在时必须实际读取，作为判断哪些模块对外发布的关键入口，不能只凭目录或类名扫描后跳过。interface 是优先线索，不是固定白名单；不能把该目录下所有内容自动判为对外，也不能排除其他目录中有发布依据的接口。对 IDL、.proto 等协议定义，继续核对生成的客户端/服务端入口、生成头文件及其构建和发布产物，不能把协议文件直接当可链接的库。",
  "从 sdk/pom.xml 追踪 modules、各子模块 pom.xml、parent、properties 和 profiles，结合 distributionManagement、发布插件配置（包括 deploy 的跳过条件）及实际发布脚本/CI 说明，区分构建聚合模块和对外发布模块。modules 是需要继续核对的候选，不是对外发布白名单；dependencies/dependencyManagement 中引用的内部模块或第三方库也不自动成为本 SDK 对外提供的能力。聚合 POM、父 POM 或 BOM 可作为发布的配置产物，但不能伪称可调用的运行时 JAR。",
  "记录发布模块实际的 groupId:artifactId:version、packaging 和必要的 classifier，将继承、属性与启用 profile 的影响说明清楚。只有配置不能证明某版本已实际发布；无法确认发布条件、依赖版本或属性值时保留待核实，不猜补。integration 中写出‘sdk/pom.xml → 子模块 POM/发布配置 → 对外制品坐标 → 对外接口’的对应关系，sources 引用实际读取的文件、行号和版本，推荐示例依赖这些对外制品；候选示例的依赖未确认时就地说明，不把未发布的实现模块当作正常接入方式。",
  "没有 sdk/pom.xml 时按仓内实际的 SDK 聚合位置或其他构建系统继续查证，不硬套 Maven；存在该文件却无法读取或发布关系不清楚时，在研究记录中明确缺口，不把它当作已确认的发布清单。仅只读分析 POM、发布脚本和文档，不执行 Maven goal 或发布命令。",
  "组件配置中的 description 可提供对外目录、入口和内部排除范围的研究提示；结合本仓文档、公开头文件安装清单、构建与发布配置、导出定义以及官方示例核对。配置路径仍是读取边界，提示不能授权越界；若关键发布证据位于配置范围外，说明缺口，请专家调整范围，不自行访问。",
  "C/C++ 核对哪些头文件实际安装或随 SDK 发布、接口与库/链接目标如何对应；Java 核对受支持的 API 包、模块 exports、SDK 文档与发布 JAR，不能把 JAR 中所有 public 类当 API；其他语言核对包的对外 exports、入口或文档约定。缺少某种机制不等于没有对外 API，不机械要求每个仓都有同名目录、导出表或同一构建系统。",
  "internal/detail/impl、未对外发布的头文件、测试夹具与 Mock、生成实现、仅组件内部协作的 public 类/方法，默认只作为核对实现的证据，不作为使用方应直接依赖的能力。不要仅凭目录名做最终判断；与文档或发布配置冲突时核对并说明适用版本。跨仓调用可能是历史违规用法，不能据此把内部实现升级成推荐接口。",
  "有研究价值但对外属性未确认的候选也可加入审核清单，默认勾选供专家筛选，不以发布证明齐全作为准入条件。此类项的标题和 interfaces 必须标注‘待核实’，内部机制的参考项标注‘内部实现参考’，写清已找到的线索、缺失证据和不应直接依赖的边界；默认勾选不等于专家已确认或推荐使用。已确认项说明受支持的入口、适用使用方与对外提供的证据，sources 保留对应路径、行号和版本。允许多分析，但不为凑数量拆出无独立用途的内部函数。",
  "最佳示例必须站在组件使用方视角，推荐写法使用受支持的入口与正常发布依赖。待核实候选也要尽力给出基于实际接口的示例，同时就地标注未确认的发布条件或依赖，不冒充可直接用于生产的推荐方案；确实缺少依据就说明缺口，不能编造 API 来补齐示例。测试可用于验证错误处理和生命周期，但不能照搬测试专用头文件、内部类或私有构建路径作为推荐用法。讨论和局部返工也须重新核对这一边界。",
  "开发范式示例要展示完整接入方式。C/C++ 代码必须包含实际的 #include <...> 或 #include \"...\"，路径按使用方安装或 SDK 导出的头文件布局填写，不只写调用函数；尽量附配套的 CMakeLists.txt cmake 代码块，列出有依据的 find_package、target_link_libraries 中的真实导出 target 或库名，必要时给出 target_include_directories。优先使用 SDK 导出的 target 传递头文件目录和依赖；没有导出 target 时按已核对的 SDK 布局说明头文件目录、库位置和链接方式，不能填写作者机器的绝对路径。核对 CMake target 与实际 .so/.a/DLL 的对应关系，target 名、链接库名和文件名不能混为一谈。纯头文件库说明无需链接二进制库；非 CMake 项目按实际构建方式给出等价接入配置，不强造 CMake 包或 target。Java 示例给出实际 import 及 pom.xml 或 Gradle 依赖，其他语言给出对应导入与包依赖。头文件、库名、target 和坐标必须从构建/安装/发布配置或真实示例核对并附来源；未找到时明确标注待核实，不猜名字，不把占位配置说成可直接构建，未经编译验证须注明。",
].join("\n");
