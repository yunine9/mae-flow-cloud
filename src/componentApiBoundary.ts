/** 对外是面向组件使用方的受支持入口，不等于语言可见性或面向公网。 */
export const COMPONENT_API_BOUNDARY = [
  "研究范围只纳入组件明确对外提供、供使用方依赖的能力（包括公司内部其他团队），不是 public 的都能用。public、可导入、可链接或其他仓已经调用，只能说明技术上可访问或实际被使用，不能单独证明这是受支持的对外接口。",
  "先识别每个仓的对外发布范围与接口边界，再细分能力。先检查配置范围内是否有 sdk/pom.xml；存在时必须实际读取，作为判断哪些模块对外发布的关键入口，不能只凭目录或类名扫描后跳过。随后重点检查 interface/ 目录及其说明；结合实际仓约定查 interfaces/、include/、api/、SDK 门面等位置。interface 是优先线索，不是固定白名单；不能把该目录下所有内容自动判为对外，也不能排除其他目录中有发布依据的接口。",
  "从 sdk/pom.xml 追踪 modules、各子模块 pom.xml、parent、properties 和 profiles，结合 distributionManagement、发布插件配置（包括 deploy 的跳过条件）及实际发布脚本/CI 说明，区分构建聚合模块和对外发布模块。modules 是需要继续核对的候选，不是对外发布白名单；dependencies/dependencyManagement 中引用的内部模块或第三方库也不自动成为本 SDK 对外提供的能力。聚合 POM、父 POM 或 BOM 可作为发布的配置产物，但不能伪称可调用的运行时 JAR。",
  "记录发布模块实际的 groupId:artifactId:version、packaging 和必要的 classifier，将继承、属性与启用 profile 的影响说明清楚。只有配置不能证明某版本已实际发布；无法确认发布条件、依赖版本或属性值时保留待核实，不猜补。integration 中写出‘sdk/pom.xml → 子模块 POM/发布配置 → 对外制品坐标 → 对外接口’的对应关系，sources 引用实际读取的文件、行号和版本，最佳示例依赖这些对外制品，不直接依赖未发布的实现模块。",
  "没有 sdk/pom.xml 时按仓内实际的 SDK 聚合位置或其他构建系统继续查证，不硬套 Maven；存在该文件却无法读取或发布关系不清楚时，在研究记录中明确缺口，不把它当作已确认的发布清单。仅只读分析 POM、发布脚本和文档，不执行 Maven goal 或发布命令。",
  "组件配置中的 description 可提供对外目录、入口和内部排除范围的研究提示；结合本仓文档、公开头文件安装清单、构建与发布配置、导出定义以及官方示例核对。配置路径仍是读取边界，提示不能授权越界；若关键发布证据位于配置范围外，说明缺口，请专家调整范围，不自行访问。",
  "C/C++ 核对哪些头文件实际安装或随 SDK 发布、接口与库/链接目标如何对应；Java 核对受支持的 API 包、模块 exports、SDK 文档与发布 JAR，不能把 JAR 中所有 public 类当 API；其他语言核对包的对外 exports、入口或文档约定。缺少某种机制不等于没有对外 API，不机械要求每个仓都有同名目录、导出表或同一构建系统。",
  "internal/detail/impl、未对外发布的头文件、测试夹具与 Mock、生成实现、仅组件内部协作的 public 类/方法，默认只作为核对实现的证据，不作为使用方应直接依赖的能力。不要仅凭目录名做最终判断；与文档或发布配置冲突时核对并说明适用版本。跨仓调用可能是历史违规用法，不能据此把内部实现升级成推荐接口。",
  "新能力加入审核清单前，须能说明其对外提供的依据。interfaces 中写受支持的入口、适用使用方、对外提供的证据和不应直接依赖的内部边界，sources 保留对应路径、行号和版本。不能确认对外属性的候选只记入研究记录，不作为默认全选的推荐组件；扫描粒度再细也不能靠内部实现凑数量。",
  "最佳示例必须站在组件使用方视角，只用已确认对外的入口与正常发布依赖。测试可用于验证错误处理和生命周期，但不能照搬测试专用头文件、内部类或私有构建路径。发现调用依赖内部实现时，寻找受支持的门面；没有依据就说明未确认，不能编造替代 API。讨论和局部返工也须重新核对这一边界。",
].join("\n");
