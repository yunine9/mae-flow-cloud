# 分支同步职责拆分

推送前同步目标分支、接续远端任务分支以及 MR 已报冲突，共用同一条 Git 准备路径。此轮按 `kernel-simplification` 的处理顺序检查后，保留实际代码冲突、远端历史完整性和凭据隔离边界；这些检查分别防止带冲突推送、覆盖他人历史、执行工作区注入的宿主凭据命令，不能用提示词代替。

删除 `taskService.ts` 中对应的实现副本，并将重复的工作区 Git 身份配置合并为一处：

- `taskBranchSync.ts`：接续任务分支、推送前合并目标分支、保留真实冲突现场，调用既有反馈和修复入口。
- `hostGitSandbox.ts`：短期凭据目录、配置与环境隔离、异步 Git 执行、输出上限和超时进程组清理。
- `taskService.ts`：绑定当前任务、操作有效期和会话调度，不再包含上述底层实现。

没有增加审批或新状态。冲突解决方法仍由实际会话收到的提示词约束；暂停、取消、已知授权以及推送结果保持既有语义。内核源码及快照均未修改，无需源仓同步。

验收覆盖首次和再次推送、MR 冲突但无流水线、运行中检视不丢失、远端新增提交、合并现场、Git 配置注入防护、临时凭据清理与知识研究源码读取。任务服务行数从 21769 降至 21229，行数上限同步收紧为 21229，不放宽或跳过推送检查。
