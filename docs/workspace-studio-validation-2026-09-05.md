# Studio 工作台实现与验证（2026-09-05）

本轮按用户确认原型实现，视觉验收以实际预览为准。入口：`http://localhost:8838/work/task-8`、`/work/task-5`。

## 已实现

- 左侧为完整材料工作区；过程文档使用目录 + Markdown 阅读器，代码默认展示文件树与差异分栏。
- 右侧优先展示当前决定/异常，其后是反馈与 Agent 回应。回复和提交通过 portal 固定在同一协作栏底部，使用原本的组件状态与权限判断。
- 运行中补充输入和开发助手使用同一底部区域；待决定时只有决定回复是主入口，高级协作保留为次要展开项。
- 单问题卡将备注和自由答复合为一个输入框，明确选择的分支优先；取消选项后文本成为自由答复。多问题与需求确认维持原有专用流程。
- 材料行内可保存或显式提交反馈，并在原位查看同一条真实批注的状态和回应；协作栏复用同一记录。发送失败时已保存记录不丢失，重试不重新创建。
- 普通反馈提交不会自动决定返工/推送；全屏检视保留显式标注的提交并返工快捷动作。最终放行、版本清单绑定、排队和中断语义继续由原 API 决定。
- 去除重复失败描述、零反馈筛选条、普通视图的第三个抽屉。保留邀请、搜索、下载、全屏、依赖图、原始事件和任务控制。
- 浅/深主题、长代码换行、窄屏目录、行内编辑器边界、底部遮挡和可见键盘焦点有独立处理。

## 验证记录

- 前端 TypeScript + Vite 生产构建通过。仍有既有主 bundle 超过 500 kB 的提醒。
- 全量根目录 `npm test`：1456 项，1444 通过、11 skip、1 既有失败。失败仍为 orderFacts 的 `UT生成方式: java-autout` 预期，与本次前端改动无关。
- 最终工作台、选择语义和检视相关定向测试：89 项通过。
- 实际浏览器检查过 1440、1280、1024、800、390 宽度；浅色/深色主题；task-8 推送检视、task-5 需求确认、task-6 多问题、task-2 运行中、task-4 暂停、task-14 失败。
- UI 验证了选择/取消返工、自由回复与按钮文案、Markdown 目录、当前材料搜索（代码命中）、全屏及检视侧栏。
- UI 创建一条测试草稿，确认原位面板与协作栏具有相同 ID，随后删除草稿；没有提交实际推送或任务决定。
- observer 账号实测：材料只读，没有可创建批注的区域和决定提交按钮；验证后已恢复 dev 账号。

16 个夹具场景由既有场景测试覆盖；未逐个向真实执行器提交破坏性动作。真实外部 CodeHub/流水线连通性不属于此次视觉验证，后端相关全量测试结果见上。

## 用户验收后调整

- 恢复旧版紫色主动作（#5b5bd6）与紫白画布，保留 Studio 面板结构和层次。
- 顶部恢复旧版节点连线、阶段名位于下方的进度条；阶段点击仍使用原执行方案入口。
- 修复“批注与检视”重复点击无响应：每次触发独立定位请求，滚动到反馈区顶部并转移焦点；普通视图不再声称展开/收起抽屉。
- 52 项相关测试通过，构建通过；浏览器实测窄屏定位以及宽屏滚回决定后再次点击定位。

## 总览与全站一致性

- 个人/团队任务及团队问题会话采用 44px 单行入口，点击直接进工作台。
- 主任务子任务数按实际任务及已确认交付单元去重，子任务保持树序、缩进及父任务导航；关联信息从筛选前的有权限集合解析。
- 紫白令牌提升为全站默认，导航和总览不再保留暖灰黄画布。
- 构建通过，36 项总览、层级与工作台测试通过。浏览器验证团队 14 行均为 44px、主任务计数显示、个人/团队行跳转，以及窄屏列布局。

## 同步 main 与桌面减负（后续）

完整合入 main `b6df7a9`，合并提交 `3d8019b`。服务端批注闭环、交付判定与子任务任务书导航一并同步；Studio 双栏、固定回复和原位批注保留。合并后前端构建、根目录类型检查和 51 项工作台专项测试通过。

按用户新要求，以 Windows 16:9 桌面尺寸为验收目标，不继续针对窄屏设计。我的需求及团队任务使用同一个行组件：推进中淡紫，待处理淡琥珀，失败淡红，完成淡绿，待合入淡青，其他淡灰；验证停摆由既有 repairStopped 语义标为待处理。每行仍保留状态文字和主子任务关联。

右栏反馈默认收成数量和待确认状态摘要；点击摘要或「批注与检视」展开同一反馈区。收起通过 hidden 保留面板实例，避免丢失未保存的编辑。当前决定和回复仍直接可见，任务详情按需展开。1366×768 验证摘要、展开、收起及团队行底色；1920×1080 验证服务端回执呈现。未提交决定、发送意见或执行重跑。

同步后全量测试：1504 项，1491 通过、11 跳过、2 失败（248.6 秒）。失败为既有 orderFacts 的 UT 生成方式断言，以及 serveConfig 固定端口 18642 被其他既存进程占用；未停止该进程。浏览器实测批注编辑→收起→展开保留未保存文字，随后取消临时编辑，未写入记录。最终前端构建通过。

最终单独复跑 serveConfig 与 5 组界面/层级测试共 48 项，全部通过；全量中的端口相关失败在复跑中未再出现。

## 检视归工作区，运行记录明确用途

按后续反馈取消右栏检视摘要与全屏检视抽屉，批注与检视改为工作区独立页签。正文与检视视图用 hidden 切换，保留未保存编辑；点击意见位置关闭检视视图并返回材料。邀请 Committer、筛选、原位批注、服务端闭环规则与全屏返工能力保留。修复 `__workspace_diff__` 虚拟批注标识误判为过程文档的问题，并增加逻辑断言。

“活动”改名“运行记录”，页签提示进度、日志、用量。页面分为进展与卡点（阶段进展默认展开）及排查与资源（执行日志、知识与上下文、模型用量、环境配置按需展开），每组说明用途。

构建及 55 项相关测试通过。1366×768 浏览器确认检视位于左侧、右栏不再挂意见面板，点击代码意见回到工作区变更并打开 src/workspace.ts；运行记录默认显示时间线摘要。未提交业务决定或批注。

## 检视与材料并排（最终修正）

用户明确要求随时弹出并对照原文。检视改为工作区内可开关的停靠面板，不再替换材料；切换材料或点击批注定位均保持面板打开，关闭后保留挂载和编辑状态。右侧决定回复区独立。全屏材料也沿用同一检视面板。1366×768 浏览器验证原文与意见并排、代码定位后面板不关闭、关闭重开正常。前端构建与 51 项相关测试通过。

## 邀请检视入口恢复

对照 main 确认邀请原本是独立入口，本轮恢复到任务头，按 canRequestReview 显示，无需打开批注面板。面板内重复入口移除，原选人对话框、权限和提交逻辑保持。更新布局测试，禁止邀请入口再依赖 reviewPanelOpen。构建和 27 项相关测试通过；1366×768 实测面板关闭时顶部邀请可见，点击打开选人表单后关闭，未发送邀请。
