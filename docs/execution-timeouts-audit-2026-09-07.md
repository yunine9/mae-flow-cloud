# 执行超时排查与修正（2026-09-07）

需求检视意见的排队和整轮修改不设时间上限。正常完成后校验逐条回执并写入原文；明确的模型、工具或校验错误才记失败。没有另加空闲看门狗，也没有把五分钟换成另一条整轮截止线。

## 根因与最终行为

| 环节 | 查到的问题与处理 |
| --- | --- |
| 需求修订 | `b8953eb6484f2bbc12baf3484e51262924ebba69`，2026-09-03 17:16:53 +0800，在新增文件编辑会话时引入固定五分钟取消。此次删除计时器及超时判定。 |
| 批注排队 | 原本不启动执行计时，但前批失败会连带恢复后续意见。此次只恢复实际失败的批次，后续继续串行执行；不会重复执行失败批次。 |
| HTTP 提交 | 已接收落盘后即回执，后台处理；不会占用整个修改回合的 HTTP 请求。 |
| 主/子 Agent 模型传输 | `a7481ce`（2026-09-06）已修复默认 300 秒断流。现有十分钟无网络数据保护保留，这是连接故障检测，不是整个任务的期限。 |
| Git 交付 | 主任务 fetch/rebase/merge/push、问题流 push 的五分钟预算与 clone 统一为三十分钟传输预算。重启凭据清理延后至六十分钟，部署等待同步延长，仍有推送进程时停止部署。 |
| 构建与预热 | 已有独立的长命令预算和整轮预算，不是五分钟。Build-Fix 中五分钟是收尾预留量，不是五分钟终止器，保持原样。 |
| 内核 codespec 适配器 | 发现 `timeout=300`，但 `kernel/MAINTAINERS.md` 和对应测试明确它是预埋未接线能力，不在当前生产执行链；没有修改内核快照或启用该能力。 |

## 验证与边界

- 实际 TaskService 修订入口搭配受控会话、虚拟时钟，等待两小时后仍可接收回执并更新原文。
- 队列分别覆盖长时间等待、前批失败而后批成功、两批各自失败、撤回后不执行。
- 定向回归包含真实 HTTP 模型传输、图文需求编辑、重启恢复、Git 与构建策略；时间推进用虚拟时钟，没有假称真实运行两小时。
- 需求修订层没有独立的死锁看门狗；如果底层永不返回且不报错，可能一直显示处理中。现有服务重启恢复会把未完成意见保留为可重新提交。
- 本次未访问内网部署现场；更新代码并重启服务后才生效。
